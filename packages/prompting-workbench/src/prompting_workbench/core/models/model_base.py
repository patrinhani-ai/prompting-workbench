from pydantic import BaseModel


class ModelBase(BaseModel):
    """
    Base class for all models in the prompting workbench.
    This class provides a common interface and basic functionality for all models.
    """
